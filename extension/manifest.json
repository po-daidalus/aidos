{
  "manifest_version": 3,
  "name": "aidos.de — Diffamierung Banner Collector",
  "version": "1.2.1",
  "description": "Collects the Google Maps 'reviews removed due to defamation' banner from place pages, with an auto-loader to sweep a list of places (resumable, per-URL check log).",
  "permissions": ["storage", "tabs", "alarms"],
  "host_permissions": ["https://www.google.com/maps/*"],
  "background": { "service_worker": "background.js" },
  "action": {
    "default_popup": "popup.html",
    "default_title": "aidos collector"
  },
  "content_scripts": [
    {
      "matches": ["https://www.google.com/maps/*"],
      "js": ["content.js"],
      "run_at": "document_idle"
    }
  ]
}
